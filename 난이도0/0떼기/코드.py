def solution(n_str):
    return n_str.lstrip('0')

## strip(문자): 인자로 전달된 문자를 String의 왼쪽과 오른쪽에서 제거
## lstrip(문자): 인자로 전달된 문자를 String의 왼쪽에서 제거
## rstrip(문자): 인자로 전달된 문자를 String의 오른쪽에서 제거(인자가 없다면 공백을 제거)
